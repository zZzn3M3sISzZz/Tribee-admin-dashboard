# Tribee API (server-side proxy + build-time fallback)
TRIBEE_API_URL=https://api.enshaproductions.com
NEXT_PUBLIC_TRIBEE_API_URL=https://api.enshaproductions.com

# Local dev: bypass cookie gate for UI review only
# NEXT_PUBLIC_AUTH_BYPASS=true

# Path-based hosting (omit for subdomain mode)
# NEXT_PUBLIC_BASE_PATH=
